vim 搜索可以是 / 或者 ?，前者是往下找，后者是往前找。
用 n 查找下一个的时候，就和这两个指令指定的方向相同。
如果你想改变方向的话，比如想往下找，那么 / 完了直接回车就行了。表示再次使用上次的搜索串。

jstack -l 9923 | grep 2c2a -A 10
获取进程号为9923的堆栈日志并获取线程号十六进程为2c2a往下10行的信息


压缩：tar -zcvf 存放打包好的文件路径及文件名 目标压缩文件或目录
解压缩：tar -zxvf
-c：创建新的tar文件
-x：解开tar文件
-z：使用gzip进行解压缩
-j：使用bzip2进行解压缩
-Z：使用compress进行解压缩
-v：显示解压缩执行过程
-f：指定要处理的文件名
